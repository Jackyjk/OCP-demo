<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.isoftstone.demo.dao.WareHouseProductDao">

    <select id="getWarehouseProductById" parameterType="int" resultType="com.isoftstone.demo.pojo.WareHouseProduct">
        SELECT `f_id` id,`f_code` code,`f_name` name,`f_model_number` modelNumber,`f_type` type,
          `f_remain_number`remainNumber,`f_price` price
        FROM `t_warehouse_product`
        WHERE `f_del_flag`=false AND `f_warehouse_id`=#{id}
    </select>
</mapper>